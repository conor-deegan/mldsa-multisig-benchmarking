//! Binius64 prover for the N-of-M ML-DSA-65 statement.
//!
//! Scaffold only. The public API consumed by `tests/xcheck.rs`
//! (`build`, `circuit_accepts`, `prove_and_verify`, and `Case`) is implemented
//! from milestone M0 onwards; see SPEC.md §1 for the frozen contract and §7 for
//! the milestone order. Until M0 lands, the oracle
//! (`cargo test -p binius-prover --test xcheck`) does not compile — by design.
//
// TODO(stub): M0 defines build / circuit_accepts / prove_and_verify / Case.
